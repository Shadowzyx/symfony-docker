<p>liste des articles</p>
<ul>
    {% for article in articles %}
        <li><a href="{{ url('specific', {'page': article}) }}" >{{ article }}</a></li>
    {% endfor %}
</ul>
<a href="{{ path('accueil') }}">accueil</a>